#!/bin/sh
# Run an editor that works.
export TMPDIR=/tmp
[ "$INSIDE_EMACS" ] && exec emacsclient -s "$EMACS_SERVER_NAME" "$@"
[ -t 0 ] && windowarg=-nw
exec emacsclient -a "" $windowarg "$@" || exec emacs "$@" || exec nano "$@"
